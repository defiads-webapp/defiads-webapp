{
  "name": "defiads-webapp",
  "version": "0.0.1",
  "description": "Defiads client based on HTML/JS/CSS stack runnable in the browser.",
  "private": true,
  "scripts": {
    "build": "yarn tsc && ./build.sh",
    "build-open": "yarn build && open target.html"
  },
  "devDependencies": {
    "@types/node": "^13.7.7",
    "@typescript-eslint/eslint-plugin": "^2.21.0",
    "@typescript-eslint/parser": "^2.21.0",
    "browserify": "^16.5.1",
    "eslint": "^6.8.0",
    "typescript": "^3.8.3"
  }
}
